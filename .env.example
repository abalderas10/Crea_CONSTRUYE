# ── Supabase ──────────────────────────────────────────────────────
# Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
# Llave publishable nueva (sb_publishable_...). Recomendada.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Alternativa: anon key legacy (JWT). Se usa si no hay publishable.
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ── Anthropic (Claude) ────────────────────────────────────────────
# Para los análisis de IA de las herramientas (fase posterior)
ANTHROPIC_API_KEY=
