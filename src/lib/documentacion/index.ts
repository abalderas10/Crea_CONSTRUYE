// Re-exports seguros para cliente: solo types y constants.
// Para funciones de servidor (upload, list, etc.) importa directamente
// desde `@/lib/documentacion/storage` (server-only).

export * from "./types";
export * from "./categories";
